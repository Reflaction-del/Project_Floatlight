# 浮光世界观编辑器 · 创作流程模拟与优化建议

> 日期：2026-08-16 ｜ 视角：世界观创作人员（设定集/小说/跑团）的完整创作动线
> 本报告基于**真实代码验证**（worldStore/FileTree/OutlineView/搜索组件逐一核对），非泛泛而谈

---

## 1. 创作流程模拟（按创作人员动线）

### 阶段一 · 开篇建世界
1. Toolbar → 世界观管理 → 新建世界（empty / novel / script 三模板）
   - ⚠️ 模板差异无预览：novel/script 会预置什么文档/实体不可见，创建后才知道

### 阶段二 · 设定搭建（骨架）
2. **实体库**：建角色/势力/地点/事件/规则 → 填字段/标签/备注 → 可选封面图
3. **关系图**：实体间连线（5 类关系）
4. **时间线**：建纪元 → 添加事件（label/year）
5. **线索板**：挂灵感线索

### 阶段三 · 正文创作（动笔）
6. **全局大纲**：卷→章→场景（任意深度）→ 状态标记 → 伏笔埋设（🔒）
7. **文档**：文件树新建文章 → TipTap 编辑 → 与大纲节点关联
   - 🔴 此处即同名 bug 爆发点：连建两篇"未命名"→ 文件树/Tab 无法区分 → 内容混淆
8. **草稿箱**：灵感快记 → 之后再整理
   - ⚠️ 草稿只能手动复制进正文，无"一键转文档"

### 阶段四 · AI 辅助（加速）
9. AI 侧栏：约束模式（RAG 检索设定）续写/灵感/考据 → 协作者模式（多步工具任务）
10. 文章抽取（粘贴素材 → 识别实体/关系 → 提案）
11. 实体消歧 / 多模态设卡 / NL→模板 / StyleKeeper 风格检测
12. 角色模拟（沙盘推演剧情）/ 跑团（AI-GM / LAN 联机）
    - ⚠️ 未配置 AI 模型时这些能力入口无统一引导

### 阶段五 · 质量把关
13. 一致性检查（自动扫描重名/悬空/孤立）
14. 提案中心（采纳/回滚）+ 执行轨迹（回放排查）
15. 伏笔账本（未回收伏笔提醒）

### 阶段六 · 产出发布
16. 视觉物料（模板/风格/批量导出 PNG/PDF）
17. 世界模板导出（.fuguworld）/ 分享 / Android / 桌面打包

---

## 2. 优化点清单（按优先级）

### P0 · 已知 Bug（已修复 ✅）
| # | 问题 | 根因 | 修复 |
|---|---|---|---|
| 1 | **同时创建两个完全相同文档 → 内容冲突** | `addDoc` 无标题查重（文件夹有 `uniqFolder` 去重、文档没有），任意重名 | ✅ 新增 `utils/docConflict.ts`：创建时同名**自动编号**「xxx（2）」并提示；重命名时**阻止冲突**。9 个单测守护（含"连续创建同名"回归用例） |

### P1 · 创作流程断点（建议下一轮做）
| # | 断点 | 现状（代码验证） | 建议 |
|---|---|---|---|
| 2 | 草稿→正文单向 | 无任何"草稿转文档"能力 | 草稿箱加「转为文档」：新建文档并填入草稿内容（保留来源标记），或并入大纲当前节点 |
| 3 | 大纲↔文档联动单向 | 节点有 docId 时才能"打开关联文档"；无 docId 无创建入口；另有"关联到首个顶层节点"的 select 简化入口**易误关联到错误节点** | 节点行加「在此创建文档并关联」（自动建文档+回填 docId）；移除/重构"首个顶层节点"入口 |
| 4 | 全局搜索缺实体/大纲 | TabBar 搜索 placeholder 仅"文档/草稿/时间轴/可视化" | 扩展索引实体名/大纲节点标题/时间线事件，结果分组展示 |
| 5 | 实体创建无即时查重 | `addEntity` 直接 push（一致性引擎事后才报"重名实体"） | 创建时同类型同名弱提示（确认继续/自动区分），降低脏数据 |

### P2 · 体验细节（打磨项）
| # | 项 | 现状 | 建议 |
|---|---|---|---|
| 6 | 保存状态不可见 | 保存按钮常驻"数据已自动保存"，无 dirty/时间指示 | 显示「未保存更改」标记 + 最后保存时间 |
| 7 | 树操作不可撤销 | 大纲/实体/文件夹删除 = confirm 后即删 | 至少大纲删除进回收/可撤销（与实体 versions 对齐） |
| 8 | Tab 无溢出管理 | tab 多时无滚动/无"关闭全部" | tab 栏加滚动 + 右键菜单（关闭其他/全部） |
| 9 | 世界模板无预览 | empty/novel/script 无差异说明 | 新建世界时展示各模板预置内容摘要 |
| 10 | AI 能力无统一引导 | 未配模型时各入口分散（侧栏提示 vs 工具栏无提示） | 全局"AI 能力锁定"状态 + 入口统一跳转设置 |
| 11 | 时间线事件不可挂实体 | 事件仅 label/year | 事件支持挂实体/文档（对齐大纲 entityIds） |
| 12 | Toolbar 命名脱节 | 「可视化编辑器」实为视觉物料生成器 | 改名「物料生成」或保留但 title 说明更直白 |

---

## 3. 建议排期

| 批次 | 内容 | 价值 |
|---|---|---|
| **批次 1（已交付）** | P0 文档同名修复 | 数据安全 |
| 批次 2 | P1-2 草稿转文档 + P1-3 大纲双向联动 | 创作动线核心断点 |
| 批次 3 | P1-4 全局搜索扩展 + P1-5 实体即时查重 | 效率 + 数据质量 |
| 批次 4 | P2 体验细节（6-12） | 打磨 |

---

## 4. 本轮交付

- ✅ **bug 修复**：`src/utils/docConflict.ts` + 测试（9 例） + `FileTree.tsx` 集成（创建自动编号+提示 / 重命名阻止冲突）
- 全量验证：**224/224 用例**通过、tsc 零错误
