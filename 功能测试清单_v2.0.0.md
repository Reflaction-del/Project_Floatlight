# 浮光世界观编辑器 v2.0.0 · 功能测试清单

> 适用版本：`v2.0.0`（Windows 便携版 `浮光世界观编辑器_v2.0.0.exe` / Android 平板版 `浮光世界观编辑器_v2.0.0.apk`）
> 测试方式：手动测试（当前未接入自动化测试）
> 使用说明：每一条在测试时勾选 `[ ] → [x]`，填写结论；结论分为 通过 / 失败 / 不适用(N/A)。

---

## 一、平台能力差异总览（先读这个）

| 功能类别 | Windows | Android | 技术原因 |
|---|---|---|---|
| 内核数据编辑 / 屏内预览 | ✅ | ✅ | 纯前端 React，两端一致 |
| 实体库 / 关系图 / 一致性检查 / 时间线 / 草稿 | ✅ | ✅ | 纯前端 |
| AI 对话、AI 生成物料（需联网+API Key） | ✅ | ✅ | 走网络请求，安卓已声明 INTERNET 权限 |
| 视觉物料**模板编辑 / 风格编辑 / 屏内实时预览** | ✅ | ✅ | 纯前端渲染 |
| 导出 PNG / PDF / Markdown 文件 | ✅ | ❌ | 依赖 `window.api.exportMaterialPng/exportMaterialPdf/exportFile`（Electron 原生桥），安卓无该桥 |
| 复制物料图片到剪贴板 | ✅ | ❌ | 依赖 `window.api.captureMaterialPng` |
| 批量 / 套系导出到文件夹 | ✅ | ❌ | 依赖 `window.api.pickFolder/materialExportBatch` |
| 本地图片上传（头像/背景/插图） | ✅ | ❌ | 依赖 `window.api.openImage` 原生对话框，安卓无此桥 |
| 系统字体列表（风格编辑器） | ✅ | ❌ | 依赖 `window.api.listFonts` |
| 导出模板预览图为 PNG | ✅ | ❌ | 依赖 `window.api.exportPreviewPng` |

> **结论**：安卓版目前只能"看"和"改"，不能"存为文件/导出/导入本地图片"。要把安卓做成完整可用，需要为 Capacitor 编写原生插件桥接（见文末"已知缺陷与待办"）。

---

## 二、Windows 版测试清单

### A. 安装与启动
- [ ] 双击 `浮光世界观编辑器_v2.0.0.exe` 可启动，无需安装、无残留注册表
- [ ] 窗口左上角显示**圆角 Logo**（22% 圆角，非旧版方块"浮"字图标）
- [ ] 任务栏图标、Alt+Tab 图标均为新圆角 Logo
- [ ] 首次启动默认进入主界面（三栏 Obsidian 式布局），无报错弹窗
- [ ] 关闭后重新打开，上次世界数据仍在（持久化正常）

### B. 全局布局与导航
- [ ] 左侧栏：实体库 / 关系图 / 一致性 / 时间线 / 草稿 / 视觉物料 / 设置 切换正常
- [ ] 三栏宽度可拖拽调整，刷新后布局保留
- [ ] 顶部标题正确显示为「浮光 · 世界观编辑器」（**无** "v1.2 预览" 字样）
- [ ] 深色 / 浅色主题切换正常，Logo 与主题协调

### C. 实体库（Entity）
- [ ] 新建实体（人员/身份/日常/情报/技术/叙事等分类下拉为**中文**）
- [ ] 编辑实体通用字段（key-value 自定义字段）保存生效
- [ ] 实体插图：上传本地图片（验证 `openImage` 原生对话框可用）
- [ ] 删除实体后有确认，且关系图中相应节点同步移除
- [ ] 搜索 / 筛选实体结果正确

### D. 关系图（Relation）
- [ ] 关系节点、连线正确渲染，可拖拽布局
- [ ] 点击节点定位到对应实体
- [ ] 关系图内支持本地图片上传（验证 `openImage`）
- [ ] 导出关系图（如有）正常

### E. 一致性检查（Consistency）
- [ ] 触发检查后列出悬空引用、重复、字段缺失等问题
- [ ] 点击问题项可跳转到对应实体/字段

### F. 时间线（Timeline）
- [ ] 时间线按时间轴正确排列事件
- [ ] 新增 / 编辑 / 删除事件生效
- [ ] 与实体、关系联动正确

### G. 草稿（Drafts）
- [ ] 新建 / 编辑 / 删除草稿
- [ ] 草稿与正式实体互转正常
- [ ] 编辑器内支持导出 Markdown（验证 `exportFile`）与导出 PDF（验证 `exportPdf`）

### H. AI 侧栏（AI）
- [ ] 配置 OpenAI 兼容协议（base_url + api_key + model）后，对话正常返回
- [ ] AI 生成内容可插入当前文档 / 实体
- [ ] 断网或 Key 错误时有明确报错，不崩溃

### I. 视觉物料生成器（MaterialForge）
- [ ] 选择模板 → 实时预览渲染正确（WYSIWYG）
- [ ] 风格编辑器：「缩放预览」滑块/加减按钮生效，范围 30%~150%，预览随缩放变化且不影响导出尺寸
- [ ] 模板「自定义背景」：纯色 / 图片 / 透明度 / 平铺方式均生效，且**预览与导出一致**
- [ ] 字段绑定八源（entity/customField/field/world/style/image/static/relation/ai）插值正确
- [ ] 分类下拉为**中文**；列表与下拉显示一致
- [ ] **导出 PNG**：尺寸与预览像素级一致（pageOverride → 风格 layout → A4 优先级正确）
- [ ] **导出 PDF**：版式与预览一致
- [ ] **复制图片**：粘贴到画图/Word 显示正确
- [ ] **导出预览图**（模板编辑器按钮）：截图与屏内预览一致
- [ ] 批量 / 套系：选择多实体 → 批量渲染 → 导出到文件夹（验证 `pickFolder` + `materialExportBatch`）
- [ ] 风格「系统字体」列表正常加载（验证 `listFonts`）

### J. 设置（Settings）
- [ ] 关于页显示版本「v2.0.0」
- [ ] 关于页下方两行小字正确显示：
  - 「浮光掠影间，不过三千世界。」
  - 「本应用由Agent智能体开发，其中所有代码均为AI生成。」
- [ ] 主题、语言等其它设置项生效并持久化

### K. 打包产物校验（发布前）
- [ ] EXE 数字信息 / 图标为新 Logo
- [ ] 安装包大小约 70MB，SHA256 记录可核对

---

## 三、Android 版测试清单（平板，建议横屏）

### A. 安装与启动
- [ ] 通过 `adb install` 或「未知来源」安装成功
- [ ] 桌面图标为圆角新 Logo
- [ ] 首次启动进入主界面，无白屏/崩溃
- [ ] 平板横屏下三栏布局正常显示，不溢出

### B. 全局布局与触摸交互
- [ ] 左右栏切换：点击生效（原桌面 hover 菜单需确认有点击替代路径）
- [ ] 滚动、双指缩放（如关系图）正常
- [ ] 软键盘弹出不遮挡输入框，可正常收起
- [ ] 屏幕旋转（横↔竖）不丢数据、不崩溃（`configChanges` 已配置）
- [ ] 无右键菜单的功能（如删除确认、上下文操作）需有长按/按钮替代

### C. 实体库 / 关系图 / 一致性 / 时间线 / 草稿
- [ ] 各模块数据增删改查正常（纯前端，预期与 Win 一致）
- [ ] 关系图拖拽、点击跳转正常
- [ ] 一致性检查、时间线联动正常

### D. AI 侧栏
- [ ] 联网 + 配 Key 后对话可用（验证 INTERNET 权限生效）
- [ ] 生成内容可插入文档

### E. 视觉物料生成器（仅预览，导出不可用）
- [ ] 模板选择、风格编辑、屏内实时预览正确（含缩放预览、自定义背景）
- [ ] 字段插值正确
- [ ] **导出 PNG / PDF / 复制图片 / 批量导出 / 导出预览图**：点击后**应提示"当前平台不支持"或静默无效**，需确认不崩溃
- [ ] 图片上传（头像/背景/插图）：当前**无原生对话框**
  - 预期现象：点击无反应或报错 → 记为已知缺陷（见待办）

### F. 设置（Settings）
- [ ] 关于页两行小字正确显示
- [ ] 版本号正确

### G. 数据持久化
- [ ] 关闭 App 再打开，数据仍在（WebView 内 localStorage/IndexedDB 持久化）
- [ ] 后台杀死进程后数据不丢失

### H. 安卓专属注意事项
- [ ] 不同分辨率/尺寸平板（如 10"/11"/12"）布局无错位
- [ ] 高刷新率屏幕无异常闪烁
- [ ] 内存占用：长时间使用无明显卡顿/崩溃
- [ ] 分享/系统返回键行为符合平板习惯

---

## 四、已知缺陷与待办（Android 完整化）

当前安卓版缺失 Electron 原生桥，以下能力不可用，需在 Capacitor 层补齐：

1. **文件导出（PNG/PDF/MD）** → 新增 Capacitor 插件，调用 Android `FileProvider`/`MediaStore` 保存并触发"下载完成"提示。
2. **图片上传（头像/背景/插图）** → 渲染层改用 `<input type="file">` Web 标准上传，作为 `window.api.openImage` 缺失时的降级方案（两端通用更稳）。
3. **文件夹选择 / 批量导出** → 用 Capacitor `Filesystem` + `FileOpener` 实现。
4. **复制图片到剪贴板** → Capacitor `Clipboard` 插件。
5. **系统字体列表** → 安卓无等价系统字体枚举，建议改为内置字体下拉（Web 安全字体 + 内置字体集）。

> 建议优先级：3（图片上传降级）> 1（文件导出）> 2（批量）> 4 > 5。完成 1+3 后安卓版即可达到"可用"水平。

---

## 五、测试结论记录

| 项目 | Windows | Android |
|---|---|---|
| 测试人 | | |
| 测试日期 | | |
| 通过数 | / | / |
| 失败数 | / | / |
| 主要遗留问题 | | |
