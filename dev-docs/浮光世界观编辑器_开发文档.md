# 浮光世界观编辑器 · 开发文档

> 版本：v2.2.9（本分支 `agent方向预览`）｜ 更新：2026-08-15
> 仓库：https://github.com/Reflaction-del/Project_Floatlight
> 性质：面向项目后续开发与维护的完整技术文档（含 Agent 化改造指引）

---

## 1. 项目概述

### 1.1 项目背景
浮光世界观编辑器（floatlight）是一款**世界观管理工具**，面向长篇创作场景，把创作者的零散灵感整合为可用产出物。产品经历了从「纯编辑器」到「AI 赋能」再到「Agent 智能体」的演进：v2.2.x 已具备完整的 AI 辅助功能闭环（提案队列、文章抽取、实体消歧、多模态设卡、NL→模板）；当前在 `agent方向预览` 分支上规划将产品从「编辑器工具」转变为**面向世界观创作者的 Agent 智能体**（详见《浮光_Agent化_系统架构设计.md》v1.2）。

### 1.2 目标用户
| 用户群 | 核心诉求 |
| --- | --- |
| 小说作者 | 管理人设/势力/地理设定，检查设定矛盾，生成视觉物料 |
| 游戏策划 | 世界观蓝图 + 一致性约束，支撑多文档创作 |
| TRPG 主持人（GM） | 规则化跑团、NPC/遭遇生成、多人同场（规划中） |

### 1.3 核心功能（现状全量）
- **实体库**：六类实体（角色/势力/地点/事件/规则），通用字段按 `customFields[key]` 映射，支持评论、版本历史（M7-3，可回滚）
- **关系图**：实体关系可视化编辑
- **时间线**：事件轴管理，`impact` 决定圆点大小
- **文档**：TipTap 富文本（表格/图片/字体/公式 KaTeX/下划线/占位/建议），文件树多层级
- **草稿箱 / 线索板**：快记与剧情线索
- **一致性检查**：设定矛盾静态检测
- **视觉物料生成器 MaterialForge**：模板+风格双体系、八源字段绑定、批量/套系、导出 PNG/PDF/SVG
- **AI 辅助 5 功能**：文章抽取 / 实体消歧 / 多模态设卡 / 提案队列+对话持久化 / NL→模板
- **主动提议（Agent 分支）**：世界变化感知，规则驱动生成合并建议提案
- **分享 / 导入导出**：`.fugu*` 纯本地格式
- **多模型接入**：OpenAI 兼容协议（base_url+api_key+model），支持云端与本地（LM Studio）

### 1.4 预期成果（Agent 化目标）
新增四项能力：① 角色模拟（多子代理自发推演，沙盘/导演双模式）② 全局树形大纲（Agent 可读写）③ 跑团模拟（规则引擎 + LAN 多人）④ 聊天平台接入（本地 API 桥接，灵感直达草稿箱）。全部保留现有功能，纯本地数据层。

---

## 2. 技术栈与架构

### 2.1 技术栈
| 层 | 选型 | 说明 |
| --- | --- | --- |
| 外壳 | Electron 31 | `electron-main.cjs` 主进程，`preload.cjs` contextBridge |
| UI 框架 | React 18 + TypeScript 5 + Vite 5 | 渲染进程 |
| 富文本 | TipTap 2 | StarterKit + Table + Image + TextStyle/Color/FontFamily + Underline + Placeholder + Suggestion |
| 样式 | Tailwind CSS 4（`@tailwindcss/vite`）+ Fluent Design System | `src/index.css` 统一主题变量 |
| 状态 | zustand 4 | 11 个 store（见 §4） |
| 存储 | 本地 JSON 文件（Electron）/ localStorage（浏览器） | `src/storage.ts` 统一抽象，无数据库 |
| 打包 | electron-builder 24.13.3 | win: nsis + portable |
| 其他依赖 | marked（markdown 渲染）、katex（公式）、qrcode、tippy.js、font-list | 见 package.json |

### 2.2 系统架构

```
┌────────────────────────────────────────────────────────────┐
│                主进程 electron-main.cjs                     │
│  BrowserWindow · IPC handler 注册 · 本机静态服务器(令牌)     │
│  原子写+备份持久化 · PDF/PNG 导出 · 窗口/标题栏管理          │
└──────────────┬───────────────────────────┬─────────────────┘
               │ contextBridge (preload.cjs)│ ipcRenderer.invoke
┌──────────────▼───────────────────────────▼─────────────────┐
│                  渲染进程（React SPA）                       │
│  组件层 components/（Toolbar/FileTree/各 Modal/视图壳）      │
│  模块层 features/（实体/关系/时间线/一致性/物料/AI/…）       │
│  状态层 store/（worldStore 单一数据源 + 各领域 store）       │
│  工具层 utils/（ai 调用/消毒/语义索引/日志/世界上下文）       │
│  存储抽象 src/storage.ts → window.api（IPC）或 localStorage  │
└────────────────────────────────────────────────────────────┘
```

**双进程职责**：
- 主进程：窗口生命周期、文件系统（原子写+备份）、本机静态服务器（随机令牌 `x-floatlight-token` + 路径穿越防护）、PDF/PNG 离屏导出、原生窗口偏好、字体枚举、菜单。
- 渲染进程：全部业务逻辑。**持久化唯一出口是 `src/storage.ts`**，Electron 环境走 `window.api`（IPC 落盘），浏览器环境回退 localStorage，业务代码无感知。

**数据流**：`组件事件 → store action（zustand set）→ storage.saveAll → IPC fs-write-file → 主进程 atomicWriteFile（.tmp+rename，带 .bak 备份）`。读取为启动时 `boot` 一次性快照注入。

### 2.3 模块划分（feature-first）
| 目录 | 职责 |
| --- | --- |
| `src/features/entities|relations|timeline|consistency|drafts|share` | 核心领域视图 |
| `src/features/materials` | 视觉物料生成器（MaterialForge） |
| `src/features/ai` | AI 能力：文章抽取/消歧/设卡/模板生成 |
| `src/features/agent` | Agent 化（主动提议引擎，后续 SubAgent/大纲/跑团） |
| `src/features/editor|settings|onboarding` | 编辑器封装/设置/新手引导 |
| `src/store` | zustand 状态层（详见 §4.2） |
| `src/utils` | 跨模块工具（AI/消毒/日志/索引） |
| `src/components` | 通用 UI 组件（AI 日志/提案中心/侧栏/各弹窗） |

---

## 3. 环境搭建

### 3.1 前置要求
- Node.js ≥ 18（开发机实测 22.x）
- Windows（主平台）
- 无环境变量要求（纯本地桌面应用，配置走文件，见 3.4）

### 3.2 安装与运行
```bash
npm install                 # 安装依赖（node_modules 已在 .gitignore）
npm run dev                # Vite 开发服务器（浏览器调试，存储走 localStorage）
npm run electron:dev       # vite build + 启动 Electron 桌面（完整功能）
npm run build              # tsc --noEmit && vite build（发版前必须零错误）
npm run dist:win           # build + electron-builder --win（nsis+portable）
```

### 3.3 配置文件说明
| 文件 | 作用 |
| --- | --- |
| `vite.config.ts` | Vite 构建配置（React 插件 + Tailwind v4） |
| `tsconfig.json` / `tsconfig.node.json` | TypeScript 编译配置 |
| `electron-main.cjs` | 主进程入口（`package.json` 的 `main` 指向） |
| `preload.cjs` | contextBridge 暴露的 `window.api`（IPC 白名单） |
| `package.json` 的 `build` 段 | electron-builder 打包配置（见 §8） |
| `build/installer.nsh` | NSIS 安装脚本（自定义进程强杀，防“无法关闭”误报） |

### 3.4 环境变量与配置
- **无 .env 体系**：桌面应用配置持久化为存储目录下的 JSON（`fl-savedir.json` 存存储位置、`fl-window-prefs.json` 存标题栏模式、`fl-ai-store-v2.json` 存 AI 模型配置、`fl-embedding.json` 存嵌入模型）。
- 模型/密钥仅存本机，由用户在设置面板录入，**不提交仓库、不硬编码**。

---

## 4. 代码结构与模块说明

### 4.1 目录总览
```
electron-main.cjs         主进程：IPC/静态服务器/导出/窗口
preload.cjs               contextBridge → window.api（25 个通道）
src/
  main.tsx / App.tsx      入口与壳（布局/主题/全局弹窗）
  types.ts                核心类型（WikiEntity/WikiRelation/Timeline/DocFile…）
  storage.ts              统一存储抽象（IPC 落盘 / localStorage 回退）
  index.css               Fluent 主题变量 + 全部组件样式
  components/             AILogPanel·ArticleImportModal·CopilotSidebar·FileTree·
                          FirstRunModal·LinkerModal·PromptModal·ProposalCenter·
                          SceneCardModal·StartPage·TabBar·TemplateGenModal·
                          TitleBar·Toolbar·icons
  features/               entities relations timeline consistency drafts share
                          materials ai agent editor settings onboarding
  store/                  worldStore aiStore worldviewStore uiStore themeStore
                          aiUsageStore appPrefs keymapStore promptStore
                          recentFilesStore proposalTypes
  utils/                  ai aiLog aiStreamWorker aiTools consistency
                          embeddingIndex image markdown sanitizeHtml share
                          worldContext
  seed/                   种子世界数据
```

### 4.2 状态层（zustand store）
| Store | 职责 | 关键方法/状态 |
| --- | --- | --- |
| `worldStore` | **核心单一数据源** | `worldsData[current]`（WorldData）；addEntity/updateEntity/deleteEntity/addRelation/deleteRelation；docs/文件树 CRUD；timelines；drafts；styles/materials/templates；**proposals 全套**（addProposal/acceptProposal/rejectProposal/acceptAllProposals/clearResolvedProposals）；**chats**（upsertChat/getChat/deleteChat）；saveNow/markDirty |
| `worldviewStore` | 世界列表 | worlds/current 的增删改、导入导出、合并 |
| `aiStore` | AI 配置 | models[]（多模型 OpenAI 兼容）/currentId/embeddingModel；持久化 fl-ai-store-v2.json |
| `aiUsageStore` | AI 用量统计 | 请求计数/token 统计 |
| `uiStore` | UI 弹窗/侧栏 | showProposals/showAILog/Copilot 开关等 |
| `themeStore` | 外观模式 | mode 持久化 fl-theme-mode（localStorage） |
| `keymapStore`/`appPrefs`/`promptStore`/`recentFilesStore` | 键位/偏好/prompt/最近文件 | 各自持久化 |

**WorldData 结构**（持久化于 fl-worlds-data.json）：`folders/docs/timelines/styles/materials/templates/entities/relations/drafts/activeDocId/activeTimelineId/clueBoard/proposals/chats`。旧文件缺字段一律 `?? []` 兜底读取。

### 4.3 工具层（utils）关键模块
| 模块 | 职责 | 关键函数 |
| --- | --- | --- |
| `ai.ts` | OpenAI 兼容调用 | `chatOnce`/`streamChat`/`extractContent`/`parseMessageFromBody`（**reasoning_content 兜底**，与 aiStreamWorker 双份实现需同步） |
| `aiStreamWorker.ts` | 流式 Worker | 与 ai.ts 解析逻辑保持一致的独立副本 |
| `aiLog.ts` + `AILogPanel` | AI 调用日志总线 | pub/sub；空响应报错附响应前 240 字 |
| `sanitizeHtml.ts` | **XSS 白名单消毒** | DOMParser 白名单；AI 回复渲染、分栏预览必过 |
| `embeddingIndex.ts` | 语义检索索引 | 向量索引 + 自动更新策略（fl-embedding） |
| `worldContext.ts` | 世界上下文打包 | 实体/关系召回给 AI 调用 |
| `aiTools.ts` | 工具调用辅助 | AI 结构化输出工具 |
| `consistency.ts` | 一致性静态规则 | 实体/关系矛盾检测 |

### 4.4 主进程 IPC 通道（window.api，见 preload.cjs）
| 通道 | 参数 → 返回 | 用途 |
| --- | --- | --- |
| `boot` | → 世界/配置全量快照（含 `embedding`，**必须显式返回否则 aiStore 启动被重置**） | 启动加载 |
| `fs-write-file` / `fs-read-file` | (name, content) → void / string|null | 通用落盘（原子写+备份） |
| `fs-get/set-save-dir` / `fs-open-save-dir` | — | 存储目录配置 |
| `fs-pick-import` / `fs-export` | — | 导入/导出对话框 |
| `open-image` / `list-fonts` | → dataURL / 字体列表 | 插图/字体 |
| `export-pdf` | (html, title) | 文档转 PDF |
| `material:capture` / `material:export-png` / `material:export-pdf` | (html, opts) → PNG dataURL / 存盘 | 离屏渲染导出 |
| `material:export-preview` | (rect, name) | 预览区所见即所得截图 |
| `material:pick-folder` / `material:export-batch` | (folder, items) | 批量导出 PNG 序列+manifest |
| `win-*` 系列 | 标题栏/窗口控制/重启/主题 | 原生窗口管理 |

### 4.5 关键调用关系（以 MaterialForge 为例）
`MaterialForgeView → Preview(MaterialPreview 实时外壳) → TemplateRenderer(全内联样式 Block 树) → previewToHtml(renderMaterialHtml 离屏序列化) → window.api.material:capture/export-* → electron-main 离屏窗口截图/打印`。
**⚠️ WYSIWYG 红线**：实时预览与离屏序列化必须共用同一套纸张外壳（`.mf-preview-frame/.mf-pv-header/.mf-pv-body/.mf-pv-footer` 类名 + 内联样式 + 相同 logo(10px)/水印(64px)/页眉(14px) 参数），修改任一边必须同步另一边，否则「导出与预览不符」。

---

## 5. 接口与数据设计

### 5.1 接口说明
本项目**无传统 HTTP 后端**。对外接口分两类：
1. **IPC 接口**：渲染进程 ↔ 主进程，通道全表见 §4.4（请求-响应式 `invoke`）。
2. **本地桥接 API（规划，Agent 化 Phase 3.5）**：主进程回环 HTTP `POST /api/v1/inspiration`（Bearer 令牌 + 限流），供聊天机器人框架（AstrBot 等）转发灵感消息 → 整理进草稿箱。契约详见《浮光_Agent化_系统架构设计.md》§4.7。

### 5.2 本地存储 Schema（无数据库，JSON 文件）
存储位置默认 `userData/worlds/`（可经设置修改，`fl-savedir.json` 记录）：

| 文件 | 内容 |
| --- | --- |
| `fl-worlds-data.json` | 全部世界数据（WorldData，见 §4.2） |
| `fl-worlds.json` | 世界列表（WorldInfo[]） |
| `fl-current-world.json` | 当前世界名 |
| `fl-ai-store-v2.json` | AI 模型配置 |
| `fl-embedding.json` | 嵌入模型 + 语义索引侧车 |
| `fl-ai-usage-v1.json` | AI 用量 |
| `fl-keymap.json` / `fl-ui-tabs.json` | 键位/标签页状态 |

**核心实体类型**（src/types.ts）：
```ts
WikiEntity { id; type(六类); name; fields[{label,value}]; custom[{label,value}];
             tags[]; note?; images[]; coverImageId?; comments?; versions[]; … }
WikiRelation { source; target; type(belongs/enemy/occurs/causal/kin/custom); label? }
DocFile { id; name; content: TipTapJSON; … }
Proposal { id; source; dedupKey?; createdAt; op; status(pending/accepted/rejected); summary }
```
约束：字段 `customFields[key]` 按 key 映射，不假设固定字段名；新字段全部可选 + `?? []` 兜底，保证旧数据兼容。

---

## 6. 核心功能实现细节

### 6.1 数据持久化（防丢失链）
```
store set → storage.saveAll → IPC fs-write-file → atomicWriteFile：
  1) 距上次备份 >5min 时先 copy 目标文件为 .bak（备份冷却）
  2) 写 .tmp → fs.renameSync 原子替换（防崩溃截断）
启动 boot 时 readJson 若主文件损坏 → 自动回退 .bak 并 console.warn
```
红线：**永不直接写目标文件**；备份失败不影响主写入；`safeName` 过滤非法文件名字符。

### 6.2 提案队列闭环（AI 写操作唯一出口）
状态流转：`pending →(acceptProposal)→ accepted` / `→(rejectProposal)→ rejected`。
- 生成：各 AI 功能（文章抽取/消歧/设卡/模板/主动提议）统一 `addProposal({source, op, summary})`。
- 采纳：`acceptProposal(id)` → `dispatchProposal(p)` 按 op.kind 分发到 worldStore（addEntity/addRelation/updateEntity/addTemplate；Agent 化新增 addTimelineEvent）。
- 主动提议（agentPropose.ts）：名称归一化精确匹配 → `buildMergePatch`（只补缺口不覆盖）→ `dedupKey` 去重（同一 key 只提一次，拒绝不再打扰）。
- **红线**：Agent/子代理/GM 均无静默写权限，所有世界库写入必经此队列。

### 6.3 AI 调用链（OpenAI 兼容 + 可观测）
`aiStore 模型配置 → utils/ai.ts chatOnce/streamChat → fetch(base_url/chat/completions) → extractContent 解析`。
- **推理模型兼容**：解析兜底 `reasoning_content`/`reasoning` 字段，否则 R1/Qwen3-Think 返回被解析为空。
- **日志**：所有调用经 aiLog 总线（URL/HTTP 状态/字符数/原始回复摘要），AILogPanel 实时展示；空响应报错附响应前 240 字并自动打开日志窗口。
- **安全**：AI 回复渲染到 DOM（marked）前必过 `sanitizeHtml` 白名单消毒，防提示词/文章注入 XSS。
- 语义检索：`embeddingIndex` 维护向量索引，`worldContext` 打包相关实体供调用。

### 6.4 主题持久化
themeStore.mode 走 `fl-theme-mode`（localStorage）；启动 init 块必须 `setState({mode}) + applyTheme(mode)`；`win-set-theme` 同步原生标题栏。

### 6.5 本机静态服务器（安全基线，dev 分支）
- 每次启动生成 32 字节随机令牌（仅内存，不落盘），`webRequest` 自动附加 `x-floatlight-token`。
- 请求校验：令牌 `timingSafeEqual` 常量时间比较 + 路径穿越防护（拒绝 `..`、解析后必须位于 DIST 内）+ SPA 回退。
- 用途：Electron 加载 `http://127.0.0.1:port` 前端资源；**Agent 化 LAN 跑团/桥接 API 的安全底座**。

---

## 7. 测试方案

### 7.1 测试框架（vitest，已引入）
- 框架：**vitest 4**（`vitest.config.ts`，node 环境，仅测纯函数与 store 逻辑，不加载 DOM/Electron）。
- 用例位置：与源码同目录（`src/**/*.test.ts`）。当前 45 用例覆盖：
  | 文件 | 守护面 |
  | --- | --- |
  | `src/utils/ai.test.ts` | AI 响应解析链：contentText/splitThinking（思考块分离）/extractContent/parseMessageFromBody（SSE+JSON 兜底、**reasoning 空串回退**）/safeParseArgs（截断 JSON）/validateModelName |
  | `src/features/agent/agentPropose.test.ts` | 主动提议引擎：normalizeName/levenshtein/isSameEntity/buildMergePatch（只补缺口不覆盖） |
  | `src/store/proposalTypes.test.ts` | 提案来源标签完整性（新增 source 必补标签） |
- **命令**：`npm test`（跑一次）/ `npm test:watch`（监听）/ `npm run check`（test + build 全量门禁）。

### 7.2 开发后自动校验（pre-commit 门禁）
- **husky 9** pre-commit hook（`.husky/pre-commit`）：每次 `git commit` 自动执行 `npm test`，**测试失败阻止提交**——「测试修正」成为开发流程的强制环节。
- 硬门槛链：`git commit → npm test → npm run build(tsc+vite)`；发版前另跑 `npx electron-builder`。
- 回归重点：**双份解析实现同步**（ai.ts 与 aiStreamWorker.ts 逻辑必须一致——测试守护 ai.ts 一侧，改任一侧需两侧同改并保持用例通过）、WYSIWYG 外壳一致性、存储兜底、XSS 消毒路径。

### 7.3 测试驱动的真实收益（记录）
引入测试后立即发现并修复一个真实缺陷：`extractContent`/`parseMessageFromBody` 用 `??` 兜底 `reasoning_content`，但推理模型常返回 `content: ""` 空串，`??` 对空串不生效 → 正文解析为空。已改为「取第一个非空串」并同步双份实现（ai.ts + aiStreamWorker.ts）。

---

## 8. 部署与运维

### 8.1 Windows 打包（electron-builder 24.13.3）
1. `npx tsc --noEmit` + `npm run build` 零错误
2. `npx electron-builder --win nsis portable`（**必须 npx**，electron-builder 不在 PATH）
3. 产物：`release/浮光世界观编辑器_vX.Y.Z.exe`（portable）+ `_Setup.exe`（NSIS）

**冲版本三处必须同步改**（否则产物名仍旧号）：
`package.json` 的 `version` + `build.nsis.artifactName` + `build.portable.artifactName`。发版后删 `release/` 被取代的旧版。

**electron-builder 已知坑**：
- `win.fileVersionInfo` / `nsis.allowDowngrade` 不是合法属性（删除）；安装包 FileDescription 取自 `package.json.description`。
- 打包前必须真正跑一次 electron-builder（仅 npm run build 不触发配置 schema 校验）。
- NSIS 中文环境误报「无法关闭」：`build/installer.nsh` 已自定义 `customCheckAppRunning`（安装/卸载前 `taskkill /f /im` 强杀）。

### 8.3 日志与监控
- **AI 调用日志**：应用内 AILogPanel（全局弹窗），AI 失败自动打开；日志经 `aiLog.ts` 总线，可扩展持久化。
- **主进程日志**：console 输出；存储损坏回退有 `[storage]` warn。
- **数据安全网**：`.bak` 备份（5min 冷却）+ 损坏自动回退。
- **常见故障排查**：
  | 症状 | 排查 |
  | --- | --- |
  | 启动后 AI 配置丢失 | 检查 `boot` 快照是否返回 `embedding`（否则 aiStore 重置） |
  | 导出与预览不符 | 检查 WYSIWYG 外壳两处是否同步 |
  | push 静默失败 | `unset HTTP_PROXY HTTPS_PROXY http_proxy https_proxy`（本机代理未运行） |
  | 世界数据损坏 | 找存储目录同名 `.bak` 恢复 |

---

## 9. 后续规划

### 9.1 Agent 化路径（详见《浮光_Agent化_系统架构设计.md》v1.2）
| Phase | 主题 | 关键交付 |
| --- | --- | --- |
| 0 ✅ | 基座 | 提案队列/chats/安全基线/主动提议 v1（已完成） |
| 1 | 认知基础 | 大纲树(任意深度)+世界快照+三级记忆 |
| 2 | 单 Agent 编排 | 工具注册表+Agent Loop+Planner |
| 3 | 多子代理·角色模拟 | SubAgent 运行时（每角色独立模型+人格）、沙盘/导演双模式、时间尺度推演 |
| 3.5 | 聊天接入 | 本地桥接 API+灵感整理管线（可与 3 并行） |
| 4 | 跑团引擎·LAN 多人 ✅ | 规则引擎+三套规则骨架+WS 房间+远程实例加入 |
| 5 | 质量增强 ✅ | StyleKeeper 风格检测+伏笔账本+插件市场雏形（世界模板 .fuguworld） |

### 9.2 已知限制
- ~~无自动化测试框架~~ ✅ 已解决（vitest 4 + pre-commit，见 §7；测试面 71 用例覆盖 AI 解析链/一致性引擎/八源绑定/主动提议/分词）
- ~~单机 JSON 存储无并发写保护~~ ✅ 已解决（electron-main 磁盘写全量串行化：`enqueueWrite` 队列包裹 8 个写路径，LAN 远程写汇入同一入口）
- 浏览器（dev）与桌面存储路径分裂（localStorage vs 文件），测试注意隔离 —— 方案待决策（见 §9.4）

### 9.3 待优化点
- 打包产物 code-split 降 chunk 体积（>500kB 警告）—— 方案待决策（见 §9.4）
- proposal 增加 provenance 溯源；事件流 checkpoint 回滚 —— 并入 Agent 化 Phase（设计已定，见《浮光_Agent化_系统架构设计.md》§4.1）
- 大纲/沙盘/跑团数据模型落地后，存储层引入 `schemaVersion` 版本化迁移 —— 随 Phase 1 落地

### 9.4 优化方案（已全部落地 ✅，2026-08-15）
| # | 项 | 方案 | 状态 |
| --- | --- | --- | --- |
| A | 存储路径分裂 | dev 模式顶部横幅「开发模式·数据存 localStorage」+ 启动写 `fl-storage-ver` 标记；迁移走 fugu* 导入导出 | ✅ 已实现 |
| B | chunk 体积 | vite `manualChunks` 拆 tiptap/katex/marked/react-vendor/vendor（视图懒加载已有），index 主包 1134kB→287kB | ✅ 已实现 |
| C | provenance + 回滚 | 并入 Agent 化 Phase 1.5/3：Proposal.provenance + 沙盘事件流 checkpoint | 随 Phase 落地 |
| D | 存储版本化 | 随 Phase 1 加 `schemaVersion` + 迁移注册表 | 随 Phase 落地 |

---

*本文档随代码演进维护；工程红线（WYSIWYG 外壳/三处冲版本/文案口径/存储兜底）与 AGENTS.md 保持一致。*
