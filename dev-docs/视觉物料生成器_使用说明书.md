# 浮光世界观编辑器 · 视觉物料生成器 使用说明书

> Visual Material Forge · 用户手册
> 适用版本：v1.6.14_preview（桌面便携版）
> 编写日期：2026-07-20

---

## 一、这是什么

**视觉物料生成器**（Visual Material Forge）是浮光世界观编辑器内置的一个产出模块。它能把你世界观里的**实体（角色 / 组织 / 物件）**与一套**美术风格**结合，自动排版出"世界观内部才会出现"的视觉物料——员工档案、证件卡、菜单通知、成员名册、海报等，并可导出 PNG / PDF / SVG 用于印刷或社交分享。

设计原则（请先记住这三点，会贯穿全文）：

1. **离线优先、本地为主**：模板、风格、导出全部在本地完成，不依赖任何云端服务。AI 是唯一可选的联网增强项，且端点由你自己填写。
2. **通用引擎、任意世界观**：风格被拆成 8 个可独立调节的维度，模板通过"字段绑定"从实体取值，因此同一套工具适用于科幻、奇幻、现代、赛博等任何世界观。
3. **AI 每张可单独开关**：任何物料都可选择"用 / 不用 AI"，AI 仅在头像、Logo、文案等增强点出现，基础排版不依赖 AI。

---

## 二、如何进入

在浮光世界观编辑器顶部标签栏选择 **「视觉物料生成器」**（替换了旧的可视化编辑器）。进入后是三栏布局：

| 区域 | 内容 |
|------|------|
| 顶栏 | 模块名 + 三个入口按钮：`市场/库`、`模板编辑器`、`导出` |
| 左栏 | **模板**列表（内置 4 个 + 我的模板）+ **风格**列表 |
| 中栏 | 实时预览（所选模板 + 所选风格 + 预览实体渲染出的物料） |
| 右栏 | **属性**面板：预览实体、头像来源、AI 增强开关、缩放、AI 文案、AI 字段补全、输出尺寸、各种产出按钮 |

---

## 三、快速上手（5 步出第一张图）

1. **选模板**：左栏「模板」下点选一个（如 `员工/角色档案`）。
2. **选预览实体**：右栏「预览实体」下拉里挑一个角色。若还没建实体，请先在编辑器「实体库」里创建。
3. **选风格**：左栏「风格」下点 `默认（自动生成）` 直接看效果，或点 `＋ 新建风格` 自定义一套。
4. **调头像来源**：右栏「头像来源」三选一（见 §6）。
5. **导出**：右栏「产出」区点 `导出 PNG（印刷）`，按保存对话框选位置即可。

---

## 四、准备工作：接入 AI（可选）

生成器里凡是带「AI」的功能（AI 头像、AI 生成 Logo、AI 文案、AI 字段补全），都需要先配置一个 **OpenAI 兼容协议的模型端点**。不配置也能用全部非 AI 功能。

配置路径：**设置 → 大模型接入 → 添加一个模型**，填写三项：
- `endpoint`：如 `https://api.openai.com/v1`（或任意兼容 `/v1` 的服务）
- `api_key`：你的密钥
- `model`：模型名，如 `gpt-4o-mini`（文生图则填支持 images 的模型）

> 文生图走 `POST {endpoint}/images/generations`；文本生成走标准 chat。若服务商不支持参考图（img2img），AI 头像会退化为纯 prompt 生成。

---

## 五、核心概念（读懂这段，后面都不难）

### 5.1 风格令牌（StyleToken）——8 个维度

"美术风格"在生成器里被拆成 8 个可独立调节的维度，每个维度都有默认值，可单独改：

| 维度 | 控制什么 |
|------|----------|
| ① 基础信息 | 风格名、标签（用于检索/推荐）、描述 |
| ② 配色 | 纸张底色 / 主文字 / 机构主色 / 次要文字 / 警示 / 提示 / 条形码色 |
| ③ 字体与字号 | 标题字体、正文字体、等宽/编号字体、三个基准字号 |
| ④ 纸张纹理 | 网格 / 纸纹 / 扫描线 / 噪点 / 点阵 / 横线 / 印章纹 / 无；不透明度 + 混合模式 |
| ⑤ 图标库 | 图标集名称与资源（当前版本图标资源以占位方式登记） |
| ⑥ 主 Logo | 机构主标识：SVG 文本 / dataURL、形状、尺寸 |
| ⑦ 签名 / 印章 | 签名字体/颜色/斜体、渲染模式、印章图片 |
| ⑧ 版式 | 画幅、宽高、页边距、内边距、页眉/页脚/水印模板 |
| ⑨ 语气词典 | 语气基调（正式/俏皮/冷峻/荒诞）+ 术语替换表 |
| ⑩ 自然语言改风格 | 用一句话调整上面各项（离线规则，不依赖 AI） |
| ⑪ 从参考图反推配色 | 上传一张图，自动取主色板填入配色 |

### 5.2 模板（Template）——声明式 Block 树

模板决定"物料长什么样、每个槽位取什么值"。内置 4 个，均 `applicableStyles:'*'`（与任意风格兼容）：

| 模板 id | 名称 | 用途 | 默认画幅 |
|---------|------|------|----------|
| `staffFile` | 员工 / 角色档案 | 照片 + 编号 + 背景记录 + 签名落款 | A4 |
| `idCard` | 证件 / ID 卡 | 带条形码、签名、头像的身份卡 | 证件卡 85.6×54mm |
| `menu` | 日常 / 菜单 | 菜单、通知、海报等日常物料 | A5 |
| `roster` | 名册 / 关系名单 | 按成员 ID 循环渲染的关系名单 | A4 |

你也可以在「模板编辑器」里新建 / 复制 / 修改模板。

### 5.3 字段绑定（FieldBinding）——物料从哪取值

模板里每个槽位的值来源（`binding.source`）共有 **8 种**：

| 来源 source | 取什么 | 常见 path 写法 |
|------------|--------|----------------|
| `entity` | 实体结构化字段 | `name` / `type` / `spectrumColor` |
| `customField` | 实体 `materialFields` 按 key 映射 | `serial` / `id` / `ai_bio` / `__key__`（表头键） |
| `world` | 世界观级信息 | `worldName` |
| `style` | 风格令牌 | `logo` / `accent` |
| `image` | 图片类 | `portrait`（按头像三模式取）/ `logo` |
| `static` | 固定文案 | 取 `binding.static` |
| `relation` | 关系数据 | （后续接入，当前回退兜底） |
| `ai` | AI 生成字段（见 §6.4） | 待补全后显示 |

**文本插值语法**（写在 TextBlock 的 `content` 里）：
- `{field:name}` → 取实体 name
- `{world:worldName}` → 取世界观名
- `{style:accent}` → 取风格主色

**条件渲染** `showIf`：某绑定值为空 / 不等于某值时整块隐藏。例如「背景记录表」仅在实体有 `materialFields` 时才显示。

> 实体自定义字段在生成器里统一走 `materialFields: Record<string,string>`（即 `customField` 按 key 映射）。在实体库中你填写的"自定义字段"会以 key→值 的形式被模板引用。

---

## 六、功能详解

### 6.1 模板与风格的选择

- **左栏「模板」**：点选即启用。用户自建模板会出现在「我的模板」分组，可「编辑」（打开模板编辑器）/「删除」。
- **左栏「风格」**：
  - `默认（自动生成）`：使用内置中性风格令牌，开箱即用。
  - 用户风格：点选即应用；「编辑」打开风格编辑器；非内置风格可「删除」（不可撤销）。
  - `＋ 新建风格`：基于默认令牌新建一套空白风格，自动打开编辑器。

### 6.2 风格编辑器（8 维 + 3 个智能辅助）

点「编辑」或「新建风格」弹出。编辑在**本地草稿**上进行，**点「保存」才生效**；点遮罩或「取消」放弃。

各分区操作要点：

- **② 配色**：每行一个取色器，点色块弹系统取色面板。
- **③ 字体与字号**：
  - 「标题字体 / 正文字体 / 等宽字体」是**带系统字体下拉的输入框**：上方直接输 `font-family`，下方下拉可「从系统字体选择」（自动列出你电脑已装字体，已去除 `(TrueType)` 等后缀并去重）+「常用字体」候选。
  - 三个字号用数字框调节。
- **④ 纸张纹理**：选纹理类型 + 不透明度（0–1）+ 混合模式（正常/正片叠底/叠加）。
- **⑥ 主 Logo**：
  - 直接粘贴 inline SVG 文本或 `data:image/...` 的 dataURL；
  - **「从本地上传 Logo」**：调系统文件选择器选一张图片，自动填入并预览；
  - 选形状（圆/椭圆/方/圆角矩形/水平条）+ 尺寸；
  - **「AI 生成 Logo」**：需先配置 AI（§4），生成 256×256 极简标志（无文字），点保存生效。
- **⑦ 签名 / 印章**：
  - 渲染模式三选一：`自动`（有图片优先显示图、否则文字）/ `仅文字` / `仅图片`；
  - 签名字体同样支持系统字体选择；
  - 颜色取色器 + 斜体开关；
  - **「印章图片源」**：可粘贴 dataURL/SVG，或**「从本地上传印章/签名」**选本地图；
  - 调印章图片高度（px）。
- **⑧ 版式**：画幅下拉（A4/A5/A6/方形/证件卡/海报/自定义）+ 宽高/边距/内边距数字 + 页眉/页脚/水印模板。页眉/页脚支持 `{worldName}` 占位（会替换成当前世界观名），水印留空则不显示。
- **⑨ 语气词典**：选语气基调（正式/俏皮/冷峻/荒诞）；可添加多组「通用占位词 → 世界观术语」替换项（如 `实验体 → Subject`），渲染时自动替换。
- **⑩ 自然语言改风格**（离线，不依赖 AI）：在文本框写一句调整指令，点「应用调整」即局部修改令牌。支持：
  - 配色：主色/纸张/文字/次要/警示/提示，可用十六进制或色名（如「深蓝」「红」「墨」）
  - 字体：标题/正文/编号（如「衬线」「黑体」「等宽」「楷体」）
  - 纹理、语气、画幅
  - 例：`主色改成深蓝，标题用衬线字体，纹理用网格，语气正式，画幅 A4`
  - 未识别会提示换种说法，已识别会列出"已应用：…"，**仍需点保存生效**。
- **⑪ 从参考图反推配色**（离线）：上传一张参考图，离线 canvas 提取主色板，自动填入纸张/墨色/主色/次要（警示/提示/条形码保持默认）。**点保存生效**。

右侧始终有**实时预览**（用 `staffFile` 模板 + 一个 Demo 角色渲染），随草稿联动，所见即所得。

### 6.3 预览实体与头像三模式

右栏「预览实体」选一个实体作为当前物料主体（照片、姓名、字段都来自它）。

「头像来源」决定照片怎么来（用户决策：实体库插图 / 用户上传 / AI 生成 三种模式）：

| 模式 | 取值逻辑 |
|------|----------|
| `entity` | 优先用实体肖像指向的插图（`portrait.imageId`），否则封面图/首张插图 |
| `upload` | 用户本地上传的图片（dataURL），点「上传头像图片」选择 |
| `ai` | AI 文生图生成的头像；点「生成 AI 头像」按当前实体+风格生成。再次生成会以**现有 AI 头像为参考图（refImage 锁）**重绘，保持同角色一致 |

> 头像模式切换不影响实体原数据，仅影响本次预览/导出。

### 6.4 AI 文案与字段补全

右栏「AI 文案」区：

- **AI 生成文案并写入字段**：选/填「写入字段 key」（默认 `ai_bio`），点按钮后用 AI 按当前实体信息 + 风格语气生成一段简介（≤120 字），写入该实体的 `materialFields[key]`。模板可用 `{customField:ai_bio}` 绑定展示。
- **AI 字段补全**：点「扫描模板并 AI 补全字段」，生成器扫描当前模板里所有声明 `source:'ai'` 的字段（如员工档案的"一句台词/格言"、证件卡的"身份格言"），逐个 AI 生成，结果进入**待审核**列表：
  - `采用` → 写入实体 `materialFields`（之后模板正常显示）；
  - `丢弃` → 不写入。
  - 未采用前这些字段在预览中按空值隐藏，不会污染数据。

### 6.5 输出尺寸

右栏「输出尺寸」下拉预设：A4 / A5 / A6 / 方形 / 证件卡 / 海报 / 跟随当前风格。选「跟随当前风格」时用风格编辑器里设置的画幅。

### 6.6 产出（右栏「产出」区）

| 按钮 | 作用 | 说明 |
|------|------|------|
| 导出 PNG（印刷） | 高清位图 | 离屏渲染 + 截图，scale=3，适合印刷 |
| 导出 PDF（印刷） | PDF 文档 | 按所选毫米尺寸排版 |
| 复制图片（社交分享） | 复制到剪贴板 | 生成 PNG 直接 `ClipboardItem` 写入，适合贴到聊天/社交 |
| 导出 SVG（矢量） | 矢量文件 | 纯渲染进程生成，下载 `.svg`，无限放大不糊 |
| 批量生成套系 | 多实体同模板 | 见 §6.7 |
| 套系 / 变量矩阵 | 多实体×多模板×多变体 | 见 §6.8 |
| 视觉一致性校验 | 质量体检 | 见 §6.9 |
| 物料画廊 | 会话成果墙 | 见 §6.10 |

### 6.7 批量生成套系

点「批量生成套系」→ 弹窗：
1. 左栏勾选要生成的**实体**（可全选/清空）。
2. 右栏选**模板**、**风格**、**头像来源**、**AI 增强**开关。
3. 点「开始批量生成」：逐张渲染（与单张导出像素级一致）。
4. 点「导出到目录」：选一个文件夹，把全部成功图 + `manifest.json` 写入该目录。

### 6.8 套系生成 · 变量矩阵

比批量更灵活，用于"同一套世界观物料出多个版本对比"。点「套系 / 变量矩阵」→ 弹窗：
- **实体**：多选。
- **套系模板**：可多选（员工档案 + 证件卡 + 菜单 + 名册 任意组合）。
- **风格**：单选（或默认）。
- **变量轴 · 头像模式**：勾选 `entity` / `upload` / `ai` 任意组合。
- **变量轴 · 含 AI 变体**：勾选后每个头像模式再各出一版 `useAI=true`。
- 预计出图数 = 实体数 × 模板数 × 变体数，实时显示。
- 「开始生成套系」后右侧画廊网格实时出缩略图；「导出到目录」把成功的图 + manifest 写入所选文件夹。

### 6.9 视觉一致性校验

点「视觉一致性校验」→ 选校验模板 + 头像模式，生成器扫描并报告：

- **风格令牌漂移 / 问题**：按 错误 / 警告 / 提示 三级列出（如缺 Logo、缺配色、字段未就绪等），每条带目标与建议。
- **资产引用计数**：
  - Logo 被多少张物料引用；
  - 头像覆盖率：每个实体在当前模式下是否有头像来源（绿点=有，灰点=缺）。

用来在批量前"体检"，避免出一沓风格不一致或缺图的物料。

### 6.10 物料画廊

当前会话内，凡是单张导出、批量、套系生成的图都会自动收进画廊（内存态，关软件清空，不持久化）。点「物料画廊」可网格浏览、单张「下载」或「删」、整体「清空」。

### 6.11 模板编辑器（可视化 + JSON）

点顶栏「模板编辑器」打开。三栏：

- **左栏**：元信息（名称/分类/画幅/适用风格`*`表示风格无关/说明）+ **块树**（可展开）+ 底部「添加块」。
  - **块类型**共 11 种：`文本 / 图片 / 表格 / 分隔线 / 组(行·列) / 循环(关系) / 条形码 / 签名 / 光谱条 / 图标 / 插槽`。
  - 选中一个「组」或「循环」后再点「添加块」，新块会进它的子级。
  - 每个块可上移 / 下移 / 删除。
- **中栏**：实时预览（用当前风格 + 你选的预览实体），可切 **JSON 模式**直接看/改模板 JSON（「载入当前」转 JSON、「应用 JSON」写回）。
- **右栏**：选中块的属性面板：
  - 文本：内容（支持 `{field:path}` 插值）、角色（title/body/label/value/caption）、字段绑定。
  - 图片：绑定、`placeholder`、圆角、宽高。
  - 表格：行来源（customFields / entityFields / static）、列编辑（表头 + 绑定路径，`__key__`/`__value__` 取行键值）。
  - 组：方向 row/col。
  - 循环：成员 ID 绑定（在实体 `materialFields` 写 `members=ID1,ID2…` 即可循环列出）。
  - 条形码/光谱/签名/图标/插槽：各自属性。
  - **通用**：每个块都能设 `showIf` 条件渲染（来源/路径/不为空才显示/等于）与行内 `style`（JSON）。

**保存规则**：
- 编辑**内置模板**时会自动复制成独立用户模板（不改内置）。
- 「保存」写入 `worldStore.templates`（持久化到世界观文件）；「另存为副本」生成新模板；用户模板可「删除」。
- 「导出 JSON / 导入 JSON」：与 `.fugutemplate` 文件互通（见 §6.12）。

> 关系循环（repeat）用法：在主体的 `materialFields` 写入 `members=实体ID1,实体ID2,…`（逗号/空格分隔），模板里的 repeat 块会据此逐个渲染子模板。

### 6.12 市场 / 库（本地，零在线依赖）

点顶栏「市场/库」打开。这是**纯本地**的模板/风格分发中心——分发的单元就是文件本身，不发任何网络请求。

- 两个标签页：**模板**（内置 + 我的）/ **风格**。
- 每个条目可：
  - `应用` → 设为当前模板/风格；
  - `导出` → 下载为 `.fugutemplate`（模板）或 `.fuguestyle`（风格）文件；
  - `删除` → 仅用户自建项可删。
- **「＋ 导入 .fugu* 文件」**：选一个本地 `.fugutemplate` / `.fuguestyle`（或 `.json`）文件，解析后落入世界库的「我的模板 / 风格」。解析是防御式的，不合法文件只温和提示，不会崩。

共享方式：把导出的 `.fugu*` 文件发给同事，对方在「市场/库」里导入即可复用你的模板/风格。

---

## 七、字段绑定语法速查

| 写法 | 含义 | 示例 |
|------|------|------|
| `binding.source='entity'`, `path='name'` | 实体名称 | 角色名 |
| `binding.source='customField'`, `path='serial'` | 实体 materialFields 的 serial 键 | 编号 |
| `binding.source='world'`, `path='worldName'` | 世界观名 | 页眉机构名 |
| `binding.source='style'`, `path='logo'` | 风格主 Logo 源 | 卡片角落 Logo |
| `binding.source='image'`, `path='portrait'` | 头像（按头像三模式取） | 照片 |
| `binding.source='static'`, `static='CONFIDENTIAL'` | 固定文案 | 页脚 |
| `binding.source='ai'`, `path='ai_quote'` | AI 生成字段（待审核后显示） | 台词/格言 |
| 文本 `content='{field:name}'` | 插值取实体字段 | 标题 |
| 文本 `content='{world:worldName}'` | 插值取世界观名 | 副标题 |
| `showIf.notEmpty` | 该绑定值非空才显示块 | 有记录才显表 |

---

## 八、内置模板字段约定（供填数据参考）

- **员工/角色档案 `staffFile`**：照片(`portrait`)、姓名(`name`)、类别(`type`)、编号(`serial`，customField)、AI 传记(`ai_bio`，customField)、背景记录表(遍历 `materialFields`)、签名(`signature`，customField)、光谱(`spectrumColor`)、AI 台词(`ai_quote`，source:'ai')。
- **证件/ID 卡 `idCard`**：机构名(`worldName`)、头像、姓名、类别、证件号(`id`)、签名(`signature`)、AI 身份格言(`ai_motto`，source:'ai')、Logo(`style.logo`)。
- **日常/菜单 `menu`**：标题(`name`)、副标(`worldName`)、品项表(遍历 `materialFields`)、说明、Logo。
- **名册/关系名单 `roster`**：标题、成员循环（主体 `materialFields.members=ID1,ID2…`）、每项照片/姓名/类别/编号。

> 提示：在实体库的「自定义字段」里填的键值，就是这些 `customField` 的 key。比如给角色加一个 `serial` 字段，员工档案的编号栏就会显示它。

---

## 九、常见问题 / 故障排查

**Q1：预览里某些块是空的 / 不显示？**
A：多半是对应字段没值。例如"背景记录表"只在实体有自定义字段时才出现；"AI 台词"未补全前本就隐藏。给实体补全字段或做 AI 字段补全即可。

**Q2：AI 按钮点了报错"未配置文生图模型"？**
A：去 **设置 → 大模型接入** 添加模型（endpoint/api_key/model），见 §4。

**Q3：系统字体下拉是空的？**
A：主进程通过系统字体接口读取；若环境无字体库或接口不可用，下拉仅显示"常用字体"候选，你仍可直接在输入框手写 `font-family`。不影响使用。

**Q4：上传的 Logo / 印章图片没生效？**
A：需点风格编辑器右下角「保存」才会写入风格；仅上传不保存只是草稿。

**Q5：导出的 PNG/PDF 尺寸不对？**
A：检查右栏「输出尺寸」与风格编辑器「版式」画幅是否冲突；选「跟随当前风格」会用风格里的设置。

**Q6：套系/批量导出到目录失败？**
A：确认至少选了 1 个实体、1 个模板、1 个变体轴；导出时要在系统对话框里选定一个**目标文件夹**并确认。

**Q7：风格编辑器左侧面板内容被截断？**
A：已修复（v1.6.14_preview 起每个折叠面板会按内容自然撑开，左栏可滚动）。如遇异常请升级到最新预览版。

**Q8：物料画廊关掉软件后空了？**
A：画廊是会话内存态，设计如此。需要长期保存请用「导出 PNG/PDF/SVG」或「批量/套系导出到目录」，模板/风格本身会随世界观文件持久化。

---

## 十、数据与文件格式

| 内容 | 存储位置 | 格式 |
|------|----------|------|
| 风格、用户模板、实体字段 | 当前世界观文件（`.world`） | 随世界观自动保存 |
| AI 模型配置 | 设置 → 大模型接入 | 本地存储 |
| 模板/风格分享文件 | 你导出的文件 | `.fugutemplate` / `.fuguestyle`（JSON 信封，含 `format`+`version`+`payload`） |
| 批量/套系导出 | 你选的目录 | 多张 `.png` + `manifest.json` |

> 离线优先：除你主动配置的 AI 端点外，生成器不会向外发送任何数据。模板/风格文件就是普通 JSON，可随手查看与备份。

---

## 十一、一句话总结工作流

> **建实体（填字段）→ 选/建模板 → 选/建风格（系统字体 + 本地 Logo/印章 + 可选参考图反推）→ 选预览实体与头像模式 → 可选 AI 补全文案 → 调输出尺寸 → 导出 PNG/PDF/SVG 或批量/套系 → 用一致性校验体检 → 成品进画廊 / 导出目录 / 用市场 .fugu* 分享。**

祝排版愉快。
